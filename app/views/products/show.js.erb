// Add the dialog title
$('#dialog h5').html("<i class='glyphicon glyphicon-plus'></i> Detail Product");

// Render the new form
$('.modal-body').html('<%= j render("detail") %>');

// Show the dynamic dialog
$('#dialog').modal("show");
